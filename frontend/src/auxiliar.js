// Función para redirigir a una página
function irA(pagina) {
  window.location.href = pagina;
}

// Función para regresar al inicio (opcional)
function regresar() {
  window.location.href = "inicio.html"; // Ajusta según tu estructura
}
